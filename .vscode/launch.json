{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (CodeLLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/app_native",
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "preLaunchTask": "Compile App (Ninja)"
    }
  ]
}
